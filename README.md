The modal visibility is controlled by toggling a css class. Event listeners are attached for click and keyboard events.
Outsiode click detection is handled by comparing the event target with teh overlay element. 
The component seperates HTML, CSS and behaviour JS.
